@extends('layouts.admin')
@section('page-title', 'Show Invoice')
@section('content')
<div class="admin-page-head"><div><a href="{{ route('admin.invoices.index') }}" class="admin-back-link">&larr; {{ trans('global.back_to_list') }}</a><h2 class="admin-page-title">Invoice Details</h2><p class="admin-page-subtitle">Invoice snapshot, items, receipts and totals</p></div><div class="show-actions">@can('invoice_edit')<a href="{{ route('admin.invoices.edit',$invoice->id) }}" class="btn-primary"><i class="fas fa-pencil-alt"></i> Edit Invoice</a>@endcan @can('invoice_print')<a href="{{ route('admin.invoices.print',$invoice->id) }}" class="btn-outline"><i class="fas fa-print"></i> Print Invoice</a>@endcan <a href="{{ route('admin.invoices.index') }}" class="btn-outline"><i class="fas fa-list"></i> Back to list</a>@can('invoice_delete')<form action="{{ route('admin.invoices.destroy',$invoice->id) }}" method="POST" onsubmit="return confirm('{{ trans('global.areYouSure') }}')">@method('DELETE')@csrf<button class="btn-danger"><i class="fas fa-trash-alt"></i> Delete Invoice</button></form>@endcan</div></div>
<div class="show-grid"><div><div class="detail-card mb-3"><div class="profile-hero"><div class="profile-avatar-lg" style="background:#4F46E5;"><i class="fas fa-file-invoice"></i></div><p class="profile-title">{{ $invoice->invoice_number }}</p><p class="profile-subtitle">{{ optional($invoice->order)->order_number ?: 'No order linked' }}</p><span class="status-pill {{ $invoice->invoice_status === 'paid' ? 'success' : 'warning' }}">{{ \App\Models\Invoice::STATUSES[$invoice->invoice_status] ?? $invoice->invoice_status }}</span></div><div class="detail-section-pad-sm"><div class="d-grid gap-2" style="grid-template-columns:1fr 1fr;"><div class="stat-mini"><p class="stat-mini-label">Total</p><p class="stat-mini-value">Rs. {{ number_format($invoice->total_amount,2) }}</p></div><div class="stat-mini"><p class="stat-mini-label">Due</p><p class="stat-mini-value">Rs. {{ number_format($invoice->due_amount,2) }}</p></div></div></div></div><div class="detail-card detail-card-pad"><p class="quick-title">Quick Actions</p><div class="quick-list">@can('invoice_edit')<a href="{{ route('admin.invoices.edit',$invoice->id) }}" class="quick-link primary"><i class="fas fa-pencil-alt"></i> Edit Invoice</a>@endcan @can('invoice_print')<a href="{{ route('admin.invoices.print',$invoice->id) }}" class="quick-link"><i class="fas fa-print"></i> Print Invoice</a>@endcan</div></div></div>
<div><div class="detail-card mb-3"><div class="detail-section-head"><div class="detail-section-icon"><i class="fas fa-info"></i></div><p class="detail-section-title">Invoice Information</p></div><div class="detail-section-body"><div class="detail-row"><span class="detail-label">Invoice Date</span><span class="detail-value">{{ optional($invoice->invoice_date)->format('d M Y') ?: '-' }}</span></div><div class="detail-row"><span class="detail-label">Due Date</span><span class="detail-value">{{ optional($invoice->due_date)->format('d M Y') ?: '-' }}</span></div><div class="detail-row"><span class="detail-label">Payment</span><span class="detail-value">{{ $invoice->payment_method ?: '-' }} / {{ $invoice->payment_status ?: '-' }}</span></div></div></div>
<div class="detail-card mb-3"><div class="detail-section-head"><div class="detail-section-icon"><i class="fas fa-user"></i></div><p class="detail-section-title">Customer Snapshot</p></div><div class="detail-section-body"><div class="detail-row"><span class="detail-label">Name</span><span class="detail-value">{{ $invoice->customer_name ?: '-' }}</span></div><div class="detail-row"><span class="detail-label">Mobile</span><span class="detail-value">{{ $invoice->customer_mobile ?: '-' }}</span></div><div class="detail-row"><span class="detail-label">Billing</span><span class="detail-value">{{ $invoice->billing_address ?: '-' }}</span></div><div class="detail-row"><span class="detail-label">Shipping</span><span class="detail-value">{{ $invoice->shipping_address ?: '-' }}</span></div></div></div>
<div class="detail-card mb-3"><div class="detail-section-head"><div class="detail-section-icon"><i class="fas fa-store"></i></div><p class="detail-section-title">Shop Snapshot</p></div><div class="detail-section-body"><div class="detail-row"><span class="detail-label">Shop</span><span class="detail-value">{{ $invoice->shop_name ?: '-' }}</span></div><div class="detail-row"><span class="detail-label">Mobile</span><span class="detail-value">{{ $invoice->shop_mobile ?: '-' }}</span></div><div class="detail-row"><span class="detail-label">Address</span><span class="detail-value">{{ $invoice->shop_address ?: '-' }}</span></div></div></div>
<div class="detail-card mb-3"><div class="detail-section-head"><div class="detail-section-icon"><i class="fas fa-list"></i></div><p class="detail-section-title">Invoice Items</p></div><div class="page-card-table"><table class="min-w-full"><thead><tr><th>Product</th><th>SKU</th><th>Size/Color</th><th>Price</th><th>Qty</th><th>Total</th></tr></thead><tbody>@forelse($invoice->items as $item)<tr><td>{{ $item->product_name }}</td><td>{{ $item->product_sku ?: '-' }}</td><td>{{ $item->size ?: '-' }} / {{ $item->color ?: '-' }}</td><td>Rs. {{ number_format($item->price,2) }}</td><td>{{ $item->quantity }}</td><td>Rs. {{ number_format($item->total,2) }}</td></tr>@empty<tr><td colspan="6">No items</td></tr>@endforelse</tbody></table></div></div>
<div class="detail-card mb-3"><div class="detail-section-head"><div class="detail-section-icon"><i class="fas fa-calculator"></i></div><p class="detail-section-title">Amount Summary</p></div><div class="detail-section-body">@foreach(['subtotal','discount_amount','tax_amount','delivery_charge','round_off','total_amount','paid_amount','due_amount'] as $field)<div class="detail-row"><span class="detail-label">{{ ucwords(str_replace('_',' ', $field)) }}</span><span class="detail-value">Rs. {{ number_format($invoice->{$field},2) }}</span></div>@endforeach</div></div>
<div class="detail-card"><div class="detail-section-head"><div class="detail-section-icon"><i class="fas fa-sticky-note"></i></div><p class="detail-section-title">Notes/Terms</p></div><div class="detail-section-body"><div class="detail-row"><span class="detail-label">Notes</span><span class="detail-value">{{ $invoice->notes ?: '-' }}</span></div><div class="detail-row"><span class="detail-label">Terms</span><span class="detail-value">{{ $invoice->terms ?: '-' }}</span></div><div class="detail-row"><span class="detail-label">Admin Note</span><span class="detail-value">{{ $invoice->admin_note ?: '-' }}</span></div></div></div></div></div>
@endsection
